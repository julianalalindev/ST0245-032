Complejidad
public void add (int elemento) {
        if(size==lista.length) {
            expandirLista();  // 2(size) +2
        }
        lista[size]=elemento;
        size++;
    }

Siendo n el número de elementos que se añaden entonces la complejidad del algoritmo es O(n x s), tal que s es el número de elementos del arreglo
